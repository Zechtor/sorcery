$newsFontSize = 18;
$newsPadding = 10;

#news {

    .article {
        display: flex;
        flex-direction: row;
        justify-content: flex-start;

        div {
            margin-bottom: ($newsPadding)px;
        }

        h3 {
            font-size: ($newsFontSize)px;
            font-weight: 600;
        }

        img {
            padding: 0px ($newsPadding)px 0px ($newsPadding)px;
        }
    }
}