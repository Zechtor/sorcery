.game {
    border-bottom: $borderStyle;
    border-left: $borderStyle;
    padding: 20px 10px;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
}

.gameInfo {
    padding: 0px 10px;
}

.gameState {
    padding: 0px 10px;
}