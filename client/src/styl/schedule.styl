#schedule {

    .game {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
     }

    .gameInfo {
        padding: 0px 10px;
    }

    .gameState {
        padding: 0px 10px;
    }   
}