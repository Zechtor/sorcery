var React = require("react");
var Layout = require("./layout");

var App = React.createClass({
    render : function() {
        return (
            <Layout />
        );
    }
});

module.exports = App;
