var Schedule = React.createClass({
    
    render : function() {
        return (
            <section className="schedule">
                Schedule
            </section>
        );
    }
});

module.exports = Schedule;
